# PROJECT_MAP.md

新しいセッションが始まった時、または相手方アカウント(本流/拡張機能どちらでも)が
何らかのアクションを起こした後、**このリポジトリで最初に見るべき場所**を示す
道しるべ。中身そのものはここには書かず、性質ごとに分かれた参照先へ誘導する。

## 4つの分類

| 種別 | 判定基準 | 例 |
|---|---|---|
| **取説(README.md)** | 今すぐ従うべきルール・今すぐ実行できる手順 | ビルド方針(ルールのみ)、ビルド手順、構成、機能別ガイド |
| **経緯(`docs/decisions/`)** | なぜそう決めたか。基本的に不変 | NDK不使用の理由、cleartext許可の理由 |
| **状態(`docs/status/`)** | 今どうなっているか。随時更新される | TODOリスト、EngineActivity整理の引き継ぎ |
| **携帯用コンテキスト(`ProjectPrompt.md`)** | 他のAI/セッションへそのままコピペして渡せる自己完結性が必須 | Claude Projectの指示文そのもの |

## 読む順番

1. **`README.md`** ← まず読む。ビルド方法・今すぐ守るべき制約が分かる
2. **`docs/status/`** ← 何を新設・改修するか決める時。作業が終わったら、
   該当項目を削除して更新すること(履歴は残さない、gitログが履歴の役目を持つ)
3. **`docs/decisions/`** ← 「なぜこの設計にしたか」の経緯を知りたい時
4. **`ProjectPrompt.md`** ← 他のAIセッションにこのプロジェクトの文脈をそのまま
   渡したい時。分割せず単一ファイルのまま使うこと

## `docs/status/`(随時更新される「今の状態」)

| ファイル | 内容 |
|---|---|
| `todo_code_comments_triage.md` | コード内のTODO/既知の制約を優先度付きで一覧化。旧README「現状のステータス」等から統合済み |
| `engine_activity_feature_split.md` | EngineActivity肥大化解消(EngineFeature化)の引き継ぎ状況 |

## `docs/decisions/`(対処済みの設計判断、基本的に不変)

`DECISION_*.md`群。実装方針そのものを覆す変更をする場合のみ更新する。

---

このファイル自体は「地図」なので、中身を`docs/status/`や`docs/decisions/`に書くのでは
なくここに書きたくなったら、それは道を誤っているサイン。このファイルは参照先の
一覧だけを保つこと。
