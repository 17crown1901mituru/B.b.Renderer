   var Thread = Packages.java.lang.Thread;
var thread = new Thread(function(){
   //Actions to be performed in this thread
   popup("Running a task in background thread");
   for(var i = 0;i<5;i++){
      Thread.sleep(1000);
   }
   popup("Finished running a task in background thread");
});
thread.start();