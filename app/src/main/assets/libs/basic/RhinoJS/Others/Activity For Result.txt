   var Gravity = Packages.android.view.Gravity;
   var Button = Packages.android.widget.Button;
   var LinearLayout = Packages.android.widget.LinearLayout;
   var Intent = Packages.android.content.Intent;
   var reqCode = 53753;

    function onCreate()
   {
      ctx.setTitle("Activity for Result");
      lay =  new LinearLayout(ctx);
      lay.setGravity(Gravity.CENTER);
      btn =  new Button(ctx);
      btn.setText("Pick file");
      btn.setOnClickListener( function ()
      {
         pickFile(reqCode);
      });
      lay.addView(btn);
      ctx.setContentView(lay);
   }

    function onActivityResult(requestCode,resultCode,data)
   {
       if (requestCode == reqCode&&data !=  null )
      {
         popup(requestCode+"\n\n"+resultCode+"\n\n"+data.getData());
      }
      
   }

    function pickFile(requestCode)
   {
      var intent =  new Intent(Intent.ACTION_GET_CONTENT);
      intent.setType("audio/*");
      ctx.startActivityForResult(intent,requestCode);
   }
   
