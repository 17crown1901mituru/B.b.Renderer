   // Saves a text, number, or boolean in persistent storage
   saveText("key1","value1");
   // Loads saved value 
   var txt = loadText("key1");
   alert(txt);
   //Same as (recommended)
   var prefs = ctx.getSharedPreferences("Main",0);
   prefs.edit().putString("key2","value2").commit();
   txt = prefs.getString("key2","");
   alert(txt);
   
