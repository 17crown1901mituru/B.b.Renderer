   var menu = ["One","Two","Three"];
   var selectedItem = 1;
   switch (selectedItem)
   {
      case 0 : print(menu[selectedItem]);
       break ;
      case 1 : print(menu[selectedItem]);
       break ;
      case 2 : print(menu[selectedItem]);
       break ;
       default  : print("none selected");
   }
   