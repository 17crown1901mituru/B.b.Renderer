   var TextToSpeech = Packages.android.speech.tts.TextToSpeech;
   let tts;
   let text = "I use JAScript app";
    function onCreate()
   {
      tts =  new TextToSpeech(ctx, function ()
      {
         tts.speak(text,TextToSpeech.QUEUE_FLUSH, null );
      });
   }
   