   var menu = ["One","Two","Three"];
   var selectedItem = 1;
   switch (selectedItem)
   {
      case 0 : console.log(menu[selectedItem]);
       break ;
      case 1 : console.log(menu[selectedItem]);
       break ;
      case 2 : console.log(menu[selectedItem]);
       break ;
       default  : console.log("none selected");
   }
   
